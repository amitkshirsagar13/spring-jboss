spring:
  application:
    name: spring-module
  cloud:
    config:
      label: master
      uri: http://localhost:8888/registry/config
      username: config
      password: config
  profiles:
    active: local
