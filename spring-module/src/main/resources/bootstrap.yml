spring:
  application:
    name: spring-module
  profiles:
    active: local
  cloud:
    config:
      label: master
      uri: http://localhost:8888/registry/config
      username: config
      password: config